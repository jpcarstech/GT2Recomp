# Autofilled by tools/new_project_layout/probe_disc.py from your legal dump.
# Review before shipping. Runtime quirks / overlay seeds still need hand work.

[game]
name = "Gran Turismo 2 (Simulation Mode) (v1.1)"
id = "SCUS-94488"
players = 2
exe = "disc/SCUS_944.88"
disc = "disc/Gran Turismo 2 (Simulation Mode) (v1.1).cue"
load_address = "0x80010000"
entry_pc = "0x8005D5C0"
text_size = "0x00099000"
stack_base = "0x00000000"

# Digests are for data Track 01 (first BINARY FILE / TRACK 01).
[prepare_disc]
out_dir = "disc"
bin_name = "Gran Turismo 2 (Simulation Mode) (v1.1).bin"
cue_name = "Gran Turismo 2 (Simulation Mode) (v1.1).cue"
boot_exe = "SCUS_944.88"
known_sizes = [680856960]
known_md5 = [
  "36d8008a99299b236a32a6bf4702aac6",
]
known_sha1 = [
  "5bd38f406bc9374335ce9f0e13b546acd64164d3",
]
known_crc32 = [
  "79233562",
]

[recompiler]
seeds = "seeds/ghidra_funcs.txt"
out_dir = "generated"
strict = true
# Same trusted entry hook as the Combined title: GT2 defeats both game-start
# detectors (HLE handoff + overlay text overwrite); entry address verified
# byte-identical across SCUS_944.55 / .88 / Combined.
mod_function_entry_funcs = ["0x8005D5C0"]

[runtime]
title_label = "Simulation"
# (no settings_path in the BUILD variant: settings.toml, input.ini, keybinds.ini
# and mods/ all sit beside the exe in build/, which is where CMake stages the
# mod packages. The installed variant, game.runtime.toml, shares them.)
window_title = "Gran Turismo 2 (Simulation Mode) (v1.1) Recompiled"
memcard_dir = "saves"
# Performance (2026-09-03, see docs/PERFORMANCE.md): the debug server's
# per-store / per-block hooks are dev tooling and cost ~14% of a race frame
# at 60 fps + 325% - silenced from boot (PSX_DEBUG_HOT_HOOKS=1 or the
# `hot_hooks` debug command re-arms them for a lab session). The framework's
# idle_skip (fast-forward through RAM-polling wait loops) is deliberately
# NOT on: measured on a fixed guest interval it made the 60 fps race 15%
# SLOWER (its per-iteration detector costs more than the few skips it
# lands; 2026-09-03, docs/PERFORMANCE.md).
debug_hot_hooks = false
# Native overlay cache on in the lab too (no autocompile command: shards are
# compiled by hand with tools/compile_overlays.py into build/cache - see
# CLAUDE.md "Rebuilding a title in the lab").
overlay_cache = true

[video]
renderer = "opengl"
aspect_ratio = "4:3"

[controller]
default_mode = "digital"

# Online mount gate: digests + track count + optional TOC fingerprint
# (see docs/NETPLAY.md).
[netplay]
require_cue = true
required_tracks = 1
required_disc_fp = "01ee5c4a52aa6625382bf1b18beb32eccc46ec23ebcf3515433a7f2a88206313"
